{
  "compilerOptions": {
    "target": "es6",
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "strict": true,
    "baseUrl": "./",
    "outDir": "./dist"
  },
  "include": ["public/ts/**/*"]
}

//"resolveJsonModule": true → Allows you to import .json files like modules.

//"esModuleInterop": true → Lets you use default imports from CommonJS modules (like your JSON).

//"include": ["ts/**/*"] → Tells TypeScript to only compile files in your ts/ folder.